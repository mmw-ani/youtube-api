MONGODB_URI = 'paste your mongo db url here';

// Paste your api key here in YOUTUBEID, if you have more than 1 API just copy others separted by comma.
YOUTUBEID = ['Paste your API key'];

module.exports = { MONGODB_URI, YOUTUBEID };
